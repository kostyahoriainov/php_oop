<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.css">
    <title>Document</title>

    <style type="text/css">
        body {
            background-color: #DADADA;
        }
        body > .grid {
            height: 100%;
        }
        .image {
            margin-top: -100px;
        }
        .column {
            max-width: 450px;
        }
    </style>
</head>
<body>

<div class="ui middle aligned center aligned grid">
    <div class="column">
        <h2 class="ui teal image header">
            <div class="content">
                Create a new account
            </div>
        </h2>
        <form class="ui large form <?= empty($error) ? '' : 'error' ?>"
              action="/sign-up/save"
              method="post">
            <div class="ui stacked segment">

                <?php if(isset($error)): ?>
                    <div class="ui error message">
                        <div class="header">Error</div>
                        <p><?= $error['message']; ?></p>
                    </div>
                <?php endif; ?>

                <div class="field">
                    <div class="ui left input <?= empty($error['first_name']) ? '' : 'field error' ?>">
                        <input type="text"
                               name="first_name"
                               value="<?= empty($request_params['first_name']) ? '' : $request_params['first_name'] ?>"
                               placeholder="First name">
                    </div>
                </div>

                <div class="field">
                    <div class="ui left input <?= empty($error['last_name']) ? '' : 'field error' ?>">
                        <input type="text"
                               name="last_name"
                               value="<?= empty($request_params['last_name']) ? '' : $request_params['last_name'] ?>"
                               placeholder="Last name">
                    </div>
                </div>


                <div class="field">
                    <div class="ui left input <?= empty($error['email']) ? '' : 'field error' ?>">
                        <input type="text"
                               name="email"
                               value="<?= empty($request_params['email']) ? '' : $request_params['email'] ?>"
                               placeholder="E-mail address">
                    </div>
                </div>

                <div class="field">
                    <div class="ui left input <?= empty($error['password']) ? '' : 'field error' ?>">
                        <input type="password"
                               name="password"
                               value="<?= empty($request_params['password']) ? '' : $request_params['password'] ?>"
                               placeholder="Password">
                    </div>
                </div>

                <div class="field">
                    <div class="ui left input <?= empty($error['password_repeat']) ? '' : 'field error' ?>">
                        <input type="password"
                               name="password_repeat"
                               value="<?= empty($request_params['password_repeat']) ? '' : $request_params['password_repeat'] ?>"
                               placeholder="Repeat your password">
                    </div>
                </div>

                <button type="submit" class="ui fluid large teal submit button">Sign Up</button>
            </div>

            <div class="ui error message"></div>

        </form>

        <div class="ui message">
            Have an account? <a href="/">Sign In</a>
        </div>
    </div>
</div>
</body>
</html>